[project]
name = "brh-rag"
version = "1.0.0"
description = "RAG Agent POC"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["agent_runtime*", "application*", "core*", "evaluation*", "llm*", "rag_agent*", "api*", "db*", "storage*", "worker*"]
